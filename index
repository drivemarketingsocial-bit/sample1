import React, { useState, useEffect, useRef } from 'react';
import { Menu, X, Sun, Moon, ChevronDown, ChevronLeft, ChevronRight, Check, MapPin, Phone, Mail, Linkedin, Instagram, Twitter, Youtube, MessageCircle, Download, TrendingUp, Users, Target, Zap, ArrowRight, Star } from 'lucide-react';

export default function DeepanshuVentures() {
  const [darkMode, setDarkMode] = useState(false);
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [activeTestimonial, setActiveTestimonial] = useState(0);
  const [openFaq, setOpenFaq] = useState(null);
  const [formData, setFormData] = useState({ name: '', email: '', phone: '', message: '' });
  const [formStatus, setFormStatus] = useState(null);
  const testimonialInterval = useRef(null);

  useEffect(() => {
    const saved = localStorage.getItem('darkMode');
    if (saved !== null) {
      setDarkMode(saved === 'true');
    } else if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
      setDarkMode(true);
    }
  }, []);

  useEffect(() => {
    localStorage.setItem('darkMode', darkMode);
    document.documentElement.classList.toggle('dark', darkMode);
  }, [darkMode]);

  useEffect(() => {
    testimonialInterval.current = setInterval(() => {
      setActiveTestimonial(prev => (prev + 1) % testimonials.length);
    }, 5000);
    return () => clearInterval(testimonialInterval.current);
  }, []);

  const scrollToSection = (id) => {
    document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' });
    setMobileMenuOpen(false);
  };

  const handleFormSubmit = (e) => {
    e.preventDefault();
    setFormStatus('success');
    setTimeout(() => setFormStatus(null), 5000);
    setFormData({ name: '', email: '', phone: '', message: '' });
  };

  const services = [
    {
      icon: <TrendingUp className="w-8 h-8" />,
      title: "Social Media Marketing",
      description: "Transform your social presence into a revenue-generating machine with data-driven campaigns.",
      benefits: [
        "Platform-specific content strategy",
        "Performance analytics & optimization",
        "Community management & engagement"
      ]
    },
    {
      icon: <Target className="w-8 h-8" />,
      title: "Performance Advertising",
      description: "ROI-focused ad campaigns across Meta, Google, and LinkedIn that deliver measurable results.",
      benefits: [
        "Creative testing & iteration",
        "Advanced audience targeting",
        "Real-time campaign optimization"
      ]
    },
    {
      icon: <Users className="w-8 h-8" />,
      title: "Personal Branding",
      description: "Build authority and trust with strategic positioning that attracts your ideal clients.",
      benefits: [
        "Thought leadership content",
        "Brand voice & messaging",
        "Multi-platform presence"
      ]
    },
    {
      icon: <Zap className="w-8 h-8" />,
      title: "Growth Consulting",
      description: "Strategic guidance to scale your business with proven frameworks and systems.",
      benefits: [
        "Marketing stack audit",
        "Growth roadmap & milestones",
        "Team training & enablement"
      ]
    }
  ];

  const caseStudies = [
    {
      client: "D2C Fashion Brand",
      problem: "Low online visibility and inconsistent sales despite quality products",
      approach: "Implemented Instagram-first strategy with influencer partnerships and performance ads",
      outcome: "3.2x revenue growth in 6 months, 45% reduction in CAC"
    },
    {
      client: "B2B SaaS Startup",
      problem: "Struggling to generate qualified leads in competitive market",
      approach: "LinkedIn thought leadership + targeted ad campaigns with ABM approach",
      outcome: "Generated 180+ qualified leads, closed 12 enterprise deals"
    },
    {
      client: "Local Service Business",
      problem: "Relying on word-of-mouth, needed scalable customer acquisition",
      approach: "Google Ads + Facebook lead gen with automated follow-up system",
      outcome: "Achieved 5:1 ROAS, booked 90 days in advance consistently"
    }
  ];

  const processSteps = [
    { title: "Discovery", description: "Deep dive into your business, audience, and goals" },
    { title: "Strategy", description: "Custom roadmap aligned with your growth objectives" },
    { title: "Creation", description: "High-performing content and campaigns that convert" },
    { title: "Launch", description: "Multi-channel execution with precision timing" },
    { title: "Optimize", description: "Continuous testing and data-driven improvements" },
    { title: "Scale", description: "Amplify what works to maximize your ROI" }
  ];

  const testimonials = [
    {
      quote: "Deepanshu transformed our social media from a cost center to our #1 revenue channel. His strategic approach and execution are world-class.",
      author: "Priya Sharma",
      role: "Founder, StyleNest",
      rating: 5
    },
    {
      quote: "Working with Deepanshu was a game-changer. He didn't just run ads—he built a complete growth system that scaled our business 4x in one year.",
      author: "Rahul Mehta",
      role: "CEO, TechFlow Solutions",
      rating: 5
    },
    {
      quote: "Finally, a marketer who understands both strategy and execution. Deepanshu delivered results that exceeded our most optimistic projections.",
      author: "Anita Desai",
      role: "Director, GreenLeaf Organics",
      rating: 5
    }
  ];

  const pricingTiers = [
    {
      name: "Starter",
      price: "₹25,000",
      period: "/month",
      features: [
        "2 social platforms",
        "12 posts per month",
        "Basic analytics",
        "Email support",
        "Monthly strategy call"
      ],
      cta: "Get Started"
    },
    {
      name: "Growth",
      price: "₹50,000",
      period: "/month",
      popular: true,
      features: [
        "4 social platforms",
        "24 posts per month",
        "Advanced analytics",
        "Performance ads (₹20k budget)",
        "Priority support",
        "Bi-weekly strategy calls",
        "Community management"
      ],
      cta: "Book a Call"
    },
    {
      name: "Custom",
      price: "Let's Talk",
      period: "",
      features: [
        "Unlimited platforms",
        "Custom content volume",
        "Dedicated account manager",
        "Enterprise analytics",
        "Full-funnel campaigns",
        "Weekly strategy sessions",
        "Team training included"
      ],
      cta: "Schedule Discovery"
    }
  ];

  const faqs = [
    {
      q: "What industries do you work with?",
      a: "I specialize in D2C brands, B2B SaaS, professional services, and local businesses across India. My frameworks adapt to your specific market dynamics."
    },
    {
      q: "How quickly can I expect results?",
      a: "Initial traction typically shows in 30-45 days. Significant results (2-3x growth) usually manifest within 3-6 months with consistent execution."
    },
    {
      q: "Do you require long-term contracts?",
      a: "I offer flexible engagements starting at 3 months to allow for meaningful impact. Month-to-month available after initial period."
    },
    {
      q: "What makes your approach different?",
      a: "I focus on the 'Domino Effect'—finding the one strategic lever that cascades into multiple wins. It's about smart execution, not just more activity."
    },
    {
      q: "Do you handle content creation?",
      a: "Yes, full-service content strategy and creation across formats—copywriting, graphics, video direction, and more."
    },
    {
      q: "What's your typical client engagement look like?",
      a: "Most clients start with the Growth package, scaling to custom solutions as we prove ROI. I work with 8-10 clients at a time to ensure quality."
    }
  ];

  const whatsappLink = "https://wa.me/919876543210?text=Hi%20Deepanshu%2C%20I'd%20like%20to%20discuss%20growth%20opportunities%20for%20my%20business.";

  return (
    <div className={`min-h-screen transition-colors duration-300 ${darkMode ? 'dark bg-gray-900 text-white' : 'bg-white text-gray-900'}`}>
      {/* Header */}
      <header className={`fixed top-0 w-full z-50 transition-all duration-300 ${darkMode ? 'bg-gray-900/95' : 'bg-white/95'} backdrop-blur-sm border-b ${darkMode ? 'border-gray-800' : 'border-gray-200'}`}>
        <nav className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-center h-16">
            <div className="text-xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
              Deepanshu Ventures
            </div>
            
            <div className="hidden md:flex items-center space-x-8">
              {['services', 'work', 'process', 'pricing', 'about', 'contact'].map(item => (
                <button
                  key={item}
                  onClick={() => scrollToSection(item)}
                  className={`capitalize hover:text-blue-600 transition-colors ${darkMode ? 'text-gray-300' : 'text-gray-700'}`}
                >
                  {item}
                </button>
              ))}
              <button
                onClick={() => setDarkMode(!darkMode)}
                className="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors"
                aria-label="Toggle dark mode"
              >
                {darkMode ? <Sun className="w-5 h-5" /> : <Moon className="w-5 h-5" />}
              </button>
            </div>

            <div className="md:hidden flex items-center space-x-2">
              <button
                onClick={() => setDarkMode(!darkMode)}
                className="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800"
                aria-label="Toggle dark mode"
              >
                {darkMode ? <Sun className="w-5 h-5" /> : <Moon className="w-5 h-5" />}
              </button>
              <button
                onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
                className="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800"
                aria-label="Toggle menu"
              >
                {mobileMenuOpen ? <X className="w-6 h-6" /> : <Menu className="w-6 h-6" />}
              </button>
            </div>
          </div>
        </nav>

        {mobileMenuOpen && (
          <div className={`md:hidden ${darkMode ? 'bg-gray-900' : 'bg-white'} border-t ${darkMode ? 'border-gray-800' : 'border-gray-200'}`}>
            <div className="px-4 py-4 space-y-3">
              {['services', 'work', 'process', 'pricing', 'about', 'contact'].map(item => (
                <button
                  key={item}
                  onClick={() => scrollToSection(item)}
                  className="block w-full text-left px-4 py-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 capitalize"
                >
                  {item}
                </button>
              ))}
            </div>
          </div>
        )}
      </header>

      {/* Hero */}
      <section className="pt-32 pb-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-4xl mx-auto text-center">
          <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
            Building Brands That Sell.<br />
            <span className="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
              Transforming Ideas Into Impact.
            </span>
          </h1>
          <p className={`text-xl mb-8 ${darkMode ? 'text-gray-300' : 'text-gray-600'}`}>
            Strategic social media marketing & growth consulting for ambitious businesses in India.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href={whatsappLink}
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center justify-center px-8 py-4 bg-green-600 hover:bg-green-700 text-white rounded-xl font-semibold transition-all transform hover:scale-105 shadow-lg"
            >
              <MessageCircle className="w-5 h-5 mr-2" />
              WhatsApp Me
            </a>
            <button
              onClick={() => scrollToSection('contact')}
              className={`inline-flex items-center justify-center px-8 py-4 rounded-xl font-semibold transition-all ${
                darkMode ? 'bg-gray-800 hover:bg-gray-700' : 'bg-gray-100 hover:bg-gray-200'
              }`}
            >
              <Download className="w-5 h-5 mr-2" />
              Download Profile
            </button>
          </div>
        </div>
      </section>

      {/* Trust Bar */}
      <section className={`py-8 border-y ${darkMode ? 'border-gray-800 bg-gray-800/50' : 'border-gray-200 bg-gray-50'}`}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex flex-wrap justify-center items-center gap-8 text-sm font-medium">
            <div className="flex items-center gap-2">
              <MapPin className="w-4 h-4 text-blue-600" />
              <span>Rohtak</span>
            </div>
            <div className="flex items-center gap-2">
              <MapPin className="w-4 h-4 text-blue-600" />
              <span>Gurugram</span>
            </div>
            <div className="flex items-center gap-2">
              <MapPin className="w-4 h-4 text-blue-600" />
              <span>Delhi-NCR</span>
            </div>
            <div className="flex items-center gap-2">
              <MapPin className="w-4 h-4 text-blue-600" />
              <span>All India</span>
            </div>
          </div>
        </div>
      </section>

      {/* Services */}
      <section id="services" className="py-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-7xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4">Services That Drive Growth</h2>
            <p className={`text-lg ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
              End-to-end marketing solutions for modern businesses
            </p>
          </div>
          <div className="grid md:grid-cols-2 gap-8">
            {services.map((service, idx) => (
              <div
                key={idx}
                className={`p-8 rounded-2xl transition-all hover:scale-105 ${
                  darkMode ? 'bg-gray-800 hover:bg-gray-750' : 'bg-white hover:shadow-xl shadow-lg'
                }`}
              >
                <div className="text-blue-600 mb-4">{service.icon}</div>
                <h3 className="text-2xl font-bold mb-3">{service.title}</h3>
                <p className={`mb-6 ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
                  {service.description}
                </p>
                <ul className="space-y-3 mb-6">
                  {service.benefits.map((benefit, i) => (
                    <li key={i} className="flex items-start gap-2">
                      <Check className="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" />
                      <span className={darkMode ? 'text-gray-300' : 'text-gray-700'}>{benefit}</span>
                    </li>
                  ))}
                </ul>
                <a
                  href={whatsappLink}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="inline-flex items-center text-blue-600 hover:text-blue-700 font-semibold"
                >
                  Book a Call <ArrowRight className="w-4 h-4 ml-2" />
                </a>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Case Studies */}
      <section id="work" className={`py-20 px-4 sm:px-6 lg:px-8 ${darkMode ? 'bg-gray-800/50' : 'bg-gray-50'}`}>
        <div className="max-w-7xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4">Proven Results</h2>
            <p className={`text-lg ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
              Real businesses, real growth
            </p>
          </div>
          <div className="grid lg:grid-cols-3 gap-8">
            {caseStudies.map((study, idx) => (
              <div
                key={idx}
                className={`p-8 rounded-2xl ${darkMode ? 'bg-gray-900' : 'bg-white'} shadow-lg`}
              >
                <h3 className="text-xl font-bold mb-4">{study.client}</h3>
                <div className="space-y-4">
                  <div>
                    <span className="text-sm font-semibold text-red-500 uppercase">Problem</span>
                    <p className={`mt-1 ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>{study.problem}</p>
                  </div>
                  <div>
                    <span className="text-sm font-semibold text-blue-500 uppercase">Approach</span>
                    <p className={`mt-1 ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>{study.approach}</p>
                  </div>
                  <div>
                    <span className="text-sm font-semibold text-green-500 uppercase">Outcome</span>
                    <p className={`mt-1 font-semibold ${darkMode ? 'text-gray-200' : 'text-gray-900'}`}>{study.outcome}</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Process */}
      <section id="process" className="py-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-7xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4">The Domino Effect System</h2>
            <p className={`text-lg ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
              Strategic marketing that creates cascading wins
            </p>
          </div>
          <div className="grid md:grid-cols-3 lg:grid-cols-6 gap-6">
            {processSteps.map((step, idx) => (
              <div key={idx} className="text-center">
                <div className={`w-16 h-16 mx-auto mb-4 rounded-full flex items-center justify-center text-xl font-bold ${
                  darkMode ? 'bg-blue-900 text-blue-300' : 'bg-blue-100 text-blue-600'
                }`}>
                  {idx + 1}
                </div>
                <h3 className="font-bold mb-2">{step.title}</h3>
                <p className={`text-sm ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
                  {step.description}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Testimonials */}
      <section className={`py-20 px-4 sm:px-6 lg:px-8 ${darkMode ? 'bg-gray-800/50' : 'bg-gray-50'}`}>
        <div className="max-w-4xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4">Client Success Stories</h2>
          </div>
          <div className="relative">
            <div className={`p-8 md:p-12 rounded-2xl ${darkMode ? 'bg-gray-900' : 'bg-white'} shadow-xl`}>
              <div className="flex justify-center mb-4">
                {[...Array(testimonials[activeTestimonial].rating)].map((_, i) => (
                  <Star key={i} className="w-5 h-5 text-yellow-400 fill-current" />
                ))}
              </div>
              <blockquote className="text-xl md:text-2xl font-medium mb-6 text-center">
                "{testimonials[activeTestimonial].quote}"
              </blockquote>
              <div className="text-center">
                <div className="font-bold">{testimonials[activeTestimonial].author}</div>
                <div className={`text-sm ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
                  {testimonials[activeTestimonial].role}
                </div>
              </div>
            </div>
            <div className="flex justify-center gap-4 mt-8">
              <button
                onClick={() => setActiveTestimonial((activeTestimonial - 1 + testimonials.length) % testimonials.length)}
                className={`p-2 rounded-full ${darkMode ? 'bg-gray-800 hover:bg-gray-700' : 'bg-white hover:bg-gray-100'} shadow-lg transition-colors`}
                aria-label="Previous testimonial"
              >
                <ChevronLeft className="w-6 h-6" />
              </button>
              <button
                onClick={() => setActiveTestimonial((activeTestimonial + 1) % testimonials.length)}
                className={`p-2 rounded-full ${darkMode ? 'bg-gray-800 hover:bg-gray-700' : 'bg-white hover:bg-gray-100'} shadow-lg transition-colors`}
                aria-label="Next testimonial"
              >
                <ChevronRight className="w-6 h-6" />
              </button>
            </div>
          </div>
        </div>
      </section>

      {/* About */}
      <section id="about" className="py-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-4xl mx-auto">
          <div className="text-center mb-12">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4">About Deepanshu Yadav</h2>
          </div>
          <div className={`p-8 md:p-12 rounded-2xl ${darkMode ? 'bg-gray-800' : 'bg-gray-50'}`}>
            <p className={`text-lg mb-6 ${darkMode ? 'text-gray-300' : 'text-gray-700'}`}>
              I help ambitious businesses unlock exponential growth through strategic marketing and brand positioning. Based in Rohtak with clients across Delhi-NCR and India, I specialize in the "Domino Effect"—identifying the one strategic lever that cascades into multiple wins.
            </p>
            <p className={`text-lg mb-6 ${darkMode ? 'text-gray-300' : 'text-gray-700'}`}>
              With a background in both agency-side execution and consulting, I bring a unique blend of strategic thinking and hands-on implementation. Whether you're a D2C brand looking to scale, a B2B company seeking qualified leads, or a founder building personal authority—I create marketing systems that deliver predictable, measurable growth.
            </p>
            <p className={`text-lg mb-8 ${darkMode ? 'text-gray-300' : 'text-gray-700'}`}>
              My approach is simple: understand your business deeply, identify high-leverage opportunities, execute with precision, and optimize relentlessly. No fluff, no vanity metrics—just results that impact your bottom line.
            </p>
            <div className="flex justify-center gap-4">
              <a href="https://linkedin.com/in/deepanshuyadav" target="_blank" rel="noopener noreferrer" className="p-3 rounded-full bg-blue-600 hover:bg-blue-700 text-white transition-colors">
                <Linkedin className="w-6 h-6" />
              </a>
              <a href="https://twitter.com/deepanshuyadav" target="_blank" rel="noopener noreferrer" className="p-3 rounded-full bg-blue-600 hover:bg-blue-700 text-white transition-colors">
                <Twitter className="w-6 h-6" />
              </a>
            </div>
          </div>
        </div>
      </section>

      {/* Pricing */}
      <section id="pricing" className={`py-20 px-4 sm:px-6 lg:px-8 ${darkMode ? 'bg-gray-800/50' : 'bg-gray-50'}`}>
        <div className="max-w-7xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4">Investment Options</h2>
            <p className={`text-lg ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
              Choose the right plan for your growth stage
            </p>
          </div>
          <div className="grid lg:grid-cols-3 gap-8">
            {pricingTiers.map((tier, idx) => (
              <div
                key={idx}
                className={`p-8 rounded-2xl relative ${
                  tier.popular 
                    ? 'ring-2 ring-blue-600 scale-105 shadow-2xl' + (darkMode ? ' bg-gray-900' : ' bg-white')
                    : (darkMode ? 'bg-gray-900' : 'bg-white') + ' shadow-lg'
                }`}
              >
                {tier.popular && (
                  <div className="absolute -top-4 left-1/2 transform -translate-x-1/2 bg-blue-600 text-white px-4 py-1 rounded-full text-sm font-semibold">
                    Most Popular
                  </div>
                )}
                <h3 className="text-2xl font-bold mb-2">{tier.name}</h3>
                <div className="mb-6">
                  <span className="text-4xl font-bold">{tier.price}</span>
                  <span className={darkMode ? 'text-gray-400' : 'text-gray-600'}>{tier.period}</span>
                </div>
                <ul className="space-y-3 mb-8">
                  {tier.features.map((feature, i) => (
                    <li key={i} className="flex items-start gap-2">
                      <Check className="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" />
                      <span className={darkMode ? 'text-gray-300' : 'text-gray-700'}>{feature}</span>
                    </li>
                  ))}
                </ul>
                <a
                  href={whatsappLink}
                  target="_blank"
                  rel="noopener noreferrer"
                  className={`block text-center py-3 px-6 rounded-xl font-semibold transition-all ${
                    tier.popular
                      ? 'bg-blue-600 hover:bg-blue-700 text-white'
                      : darkMode
                      ? 'bg-gray-800 hover:bg-gray-700'
                      : 'bg-gray-100 hover:bg-gray-200'
                  }`}
                >
                  {tier.cta}
                </a>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* FAQ */}
      <section className="py-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-3xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4">Frequently Asked Questions</h2>
          </div>
          <div className="space-y-4">
            {faqs.map((faq, idx) => (
              <div
                key={idx}
                className={`rounded-xl overflow-hidden ${darkMode ? 'bg-gray-800' : 'bg-gray-50'}`}
              >
                <button
                  onClick={() => setOpenFaq(openFaq === idx ? null : idx)}
                  className="w-full px-6 py-4 flex justify-between items-center text-left"
                >
                  <span className="font-semibold">{faq.q}</span>
                  <ChevronDown className={`w-5 h-5 transition-transform ${openFaq === idx ? 'rotate-180' : ''}`} />
                </button>
                {openFaq === idx && (
                  <div className={`px-6 pb-4 ${darkMode ? 'text-gray-300' : 'text-gray-600'}`}>
                    {faq.a}
                  </div>
                )}
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Contact Form */}
      <section id="contact" className={`py-20 px-4 sm:px-6 lg:px-8 ${darkMode ? 'bg-gray-800/50' : 'bg-gray-50'}`}>
        <div className="max-w-2xl mx-auto">
          <div className="text-center mb-12">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4">Let's Build Something Great</h2>
            <p className={`text-lg ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
              Ready to transform your marketing? Get in touch.
            </p>
          </div>
          <div className={`p-8 rounded-2xl ${darkMode ? 'bg-gray-900' : 'bg-white'} shadow-xl`}>
            <form onSubmit={handleFormSubmit} className="space-y-6">
              <div>
                <label className="block mb-2 font-semibold">Name</label>
                <input
                  type="text"
                  required
                  value={formData.name}
                  onChange={(e) => setFormData({...formData, name: e.target.value})}
                  className={`w-full px-4 py-3 rounded-lg ${darkMode ? 'bg-gray-800 border-gray-700' : 'bg-gray-50 border-gray-300'} border focus:ring-2 focus:ring-blue-500 outline-none transition-all`}
                  placeholder="Your name"
                />
              </div>
              <div>
                <label className="block mb-2 font-semibold">Email</label>
                <input
                  type="email"
                  required
                  value={formData.email}
                  onChange={(e) => setFormData({...formData, email: e.target.value})}
                  className={`w-full px-4 py-3 rounded-lg ${darkMode ? 'bg-gray-800 border-gray-700' : 'bg-gray-50 border-gray-300'} border focus:ring-2 focus:ring-blue-500 outline-none transition-all`}
                  placeholder="your@email.com"
                />
              </div>
              <div>
                <label className="block mb-2 font-semibold">Phone</label>
                <input
                  type="tel"
                  required
                  value={formData.phone}
                  onChange={(e) => setFormData({...formData, phone: e.target.value})}
                  className={`w-full px-4 py-3 rounded-lg ${darkMode ? 'bg-gray-800 border-gray-700' : 'bg-gray-50 border-gray-300'} border focus:ring-2 focus:ring-blue-500 outline-none transition-all`}
                  placeholder="+91 98765 43210"
                />
              </div>
              <div>
                <label className="block mb-2 font-semibold">Message</label>
                <textarea
                  required
                  value={formData.message}
                  onChange={(e) => setFormData({...formData, message: e.target.value})}
                  rows="4"
                  className={`w-full px-4 py-3 rounded-lg ${darkMode ? 'bg-gray-800 border-gray-700' : 'bg-gray-50 border-gray-300'} border focus:ring-2 focus:ring-blue-500 outline-none transition-all resize-none`}
                  placeholder="Tell me about your project..."
                ></textarea>
              </div>
              <button
                type="submit"
                className="w-full py-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-xl transition-all transform hover:scale-105 shadow-lg"
              >
                Send Message
              </button>
            </form>
            {formStatus === 'success' && (
              <div className="mt-4 p-4 bg-green-500/10 border border-green-500 rounded-lg text-green-500 text-center">
                ✓ Message sent! I'll get back to you soon.
              </div>
            )}
            <div className="mt-8 pt-8 border-t border-gray-700">
              <p className="text-center mb-4 font-semibold">Or reach out directly:</p>
              <div className="flex flex-col sm:flex-row gap-4 justify-center">
                <a
                  href={whatsappLink}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="inline-flex items-center justify-center px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-semibold transition-all"
                >
                  <MessageCircle className="w-5 h-5 mr-2" />
                  WhatsApp
                </a>
                <a
                  href="mailto:deepanshu@ventures.com"
                  className={`inline-flex items-center justify-center px-6 py-3 rounded-lg font-semibold transition-all ${darkMode ? 'bg-gray-800 hover:bg-gray-700' : 'bg-gray-100 hover:bg-gray-200'}`}
                >
                  <Mail className="w-5 h-5 mr-2" />
                  Email
                </a>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className={`py-12 px-4 sm:px-6 lg:px-8 border-t ${darkMode ? 'border-gray-800 bg-gray-900' : 'border-gray-200 bg-gray-50'}`}>
        <div className="max-w-7xl mx-auto">
          <div className="grid md:grid-cols-3 gap-8 mb-8">
            <div>
              <h3 className="text-xl font-bold mb-4 bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
                Deepanshu Ventures
              </h3>
              <p className={darkMode ? 'text-gray-400' : 'text-gray-600'}>
                Strategic marketing & growth consulting for ambitious businesses across India.
              </p>
            </div>
            <div>
              <h4 className="font-bold mb-4">Quick Links</h4>
              <div className="space-y-2">
                {['services', 'work', 'pricing', 'about', 'contact'].map(item => (
                  <button
                    key={item}
                    onClick={() => scrollToSection(item)}
                    className={`block capitalize hover:text-blue-600 transition-colors ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}
                  >
                    {item}
                  </button>
                ))}
              </div>
            </div>
            <div>
              <h4 className="font-bold mb-4">Connect</h4>
              <div className="flex gap-4 mb-4">
                <a href="https://linkedin.com/in/deepanshuyadav" target="_blank" rel="noopener noreferrer" className="p-2 rounded-lg hover:bg-blue-600 hover:text-white transition-colors">
                  <Linkedin className="w-5 h-5" />
                </a>
                <a href="https://instagram.com/deepanshuyadav" target="_blank" rel="noopener noreferrer" className="p-2 rounded-lg hover:bg-pink-600 hover:text-white transition-colors">
                  <Instagram className="w-5 h-5" />
                </a>
                <a href="https://twitter.com/deepanshuyadav" target="_blank" rel="noopener noreferrer" className="p-2 rounded-lg hover:bg-blue-400 hover:text-white transition-colors">
                  <Twitter className="w-5 h-5" />
                </a>
                <a href="https://youtube.com/@deepanshuyadav" target="_blank" rel="noopener noreferrer" className="p-2 rounded-lg hover:bg-red-600 hover:text-white transition-colors">
                  <Youtube className="w-5 h-5" />
                </a>
              </div>
              <div className={`text-sm ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
                <p className="flex items-center gap-2 mb-2">
                  <MapPin className="w-4 h-4" />
                  Rohtak, Gurugram, Delhi-NCR
                </p>
                <p className="flex items-center gap-2">
                  <Phone className="w-4 h-4" />
                  +91 98765 43210
                </p>
              </div>
            </div>
          </div>
          <div className={`pt-8 border-t ${darkMode ? 'border-gray-800' : 'border-gray-200'} text-center ${darkMode ? 'text-gray-400' : 'text-gray-600'}`}>
            <p>© {new Date().getFullYear()} Deepanshu Ventures. All rights reserved.</p>
          </div>
        </div>
      </footer>
    </div>
  );
}
